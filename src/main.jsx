import React from "react";
import ReactDOM from "react-dom/client";
import InstaClean from "./InstaClean.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InstaClean />
  </React.StrictMode>
);
